## 1. Setup
Clone o repositório e execute o script de inicialização:

```bash
git clone https://github.com/gabrielpereiraa/app-interview-tray.git
cd app-interview-tray
cp .env.example .env
npm i && npm run dev
```
